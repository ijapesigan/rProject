---
title: "Website Build Session"
author: "Ivan Jacob Agaloos Pesigan"
---

## Session

```{r}
sessionInfo()
```

```{r}
#| echo: false
build <- list.files(
  "/srv/build"
)
for (i in seq_along(build)) {
  cat(
    paste0(
      build[i],
      ":\n"
    )
  )
  readLines(build[i])
}
```
