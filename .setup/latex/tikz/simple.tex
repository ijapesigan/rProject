\documentclass[tikz, border = 1mm]{standalone}

\begin{document}

\usetikzlibrary{
	er,
	arrows,
	positioning
}

\begin{tikzpicture}[
		auto,
		node distance = 20mm,
		manifest/.style = {
			rectangle,
			draw,
			thick,
			inner sep = 5pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		inv/.style = {
			rectangle,
			draw=none,
			fill=none,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		error/.style = {
			ellipse,
			draw,
			thick,
			inner sep = 0pt,
			minimum size = 7mm,
			align = center
		},
		mean/.style={
			regular polygon,
			regular polygon sides = 3,
			draw,
			thick,
			inner sep = 0pt,
			minimum width = 7mm,
			minimum height = 7mm
		},
		path/.style = {
			->,
			thick,
			>=stealth'
		},
		cov/.style = {
			<->,
			thick,
			>=stealth'
		},
	]
	\node[manifest] (X) {Stimulus ($X$)};
	\node[manifest] (M) [above right = of X] {Cognition ($M$)};
	\node[manifest] (Y) [below right = of M] {Response ($Y$)};
	\draw[path] (M) to node[above] {$\beta$} (Y);
	\draw[path] (X) to node {$\tau^{\prime}$} (Y);
	\draw[path] (X) to node {$\alpha$} (M);
\end{tikzpicture}

\end{document}
